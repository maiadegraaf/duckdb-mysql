# name: test/sql/connection_pool/connection_pool_settings.test
# description: Test connection pool configuration settings
# group: [connection_pool]

require mysql_scanner

require-env MYSQL_TEST_DATABASE_AVAILABLE

# First attach to ensure extension is fully loaded
statement ok
ATTACH 'host=localhost user=root port=0 database=mysql' AS s_default (TYPE MYSQL_SCANNER)

# Test that pool settings exist and have sensible defaults
query I
SELECT current_setting('mysql_pool_size')::INTEGER BETWEEN 4 AND 32
----
true

query I
SELECT current_setting('mysql_pool_wait_timeout_millis')
----
30000

# Basic query to verify connection works
statement ok
SELECT 1 FROM s_default.information_schema.tables LIMIT 1

statement ok
DETACH s_default

# Test pool size of 1 (minimal pool) via SET
statement ok
SET mysql_pool_size = 1

query I
SELECT current_setting('mysql_pool_size')
----
1

statement ok
ATTACH 'host=localhost user=root port=0 database=mysql' AS s2 (TYPE MYSQL_SCANNER)

statement ok
SELECT 1 FROM s2.information_schema.tables LIMIT 1

statement ok
DETACH s2

# Test custom timeout
statement ok
SET mysql_pool_wait_timeout_millis = 60000

query I
SELECT current_setting('mysql_pool_wait_timeout_millis')
----
60000

# Reset to defaults
statement ok
RESET mysql_pool_size

statement ok
RESET mysql_pool_wait_timeout_millis
